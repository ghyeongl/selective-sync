{
  "name": "e2e-sync-tests",
  "private": true,
  "scripts": {
    "build": "cd ../filebrowser && go build -o filebrowser-darwin-arm64 .",
    "pretest": "npm run build",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
}
